server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/end_to_end_db?useSSL=false&serverTimezone=UTC
    username: root
    password: #your local SQL instance password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  thymeleaf:
    cache: false

  mail:
    host: smtp.gmail.com
    port: 587
    username: jimohabdulquyum@gmail.com
    password: "password"   # <-- replace with your Gmail APP PASSWORD, not normal Gmail password
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

logging:
  level:
    org.springframework: INFO
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
